#!/usr/bin/env python3
"""Visualization demo retired.

Leverage ``run_experiment.py`` with appropriate visualization overrides (e.g.
``visualization.level=full``) or build bespoke notebooks for ad-hoc demos.
"""


def main() -> None:
    raise RuntimeError(
        "scripts/demo_visualizations.py has been retired. "
        "Use run_experiment.py with visualization overrides or custom notebooks."
    )


if __name__ == "__main__":
    main()
