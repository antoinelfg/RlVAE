# @package experiment

# RHMC disabled (z0-only). Stronger shrinkage of Σ: α=0.15, ε=1e-3

defaults:
  - rlvae_three_stage_long_rhmc_modular_steps0

model:
  posterior:
    type: riemannian_rhmc
    rhmc_steps: 0
    rhmc_alpha: 0.15
    rhmc_eps_reg: 1e-3
  rhmc_steps: 0
  rhmc_alpha: 0.15
  rhmc_eps_reg: 1e-3
  posterior_local_alpha: 0.15

training:
  model:
    posterior:
      type: riemannian_rhmc
      rhmc_steps: 0
      rhmc_alpha: 0.15
      rhmc_eps_reg: 1e-3

