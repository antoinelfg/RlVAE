# @package experiment

defaults:
  - rlvae_three_stage_long_rhmc_modular

# Force RHMC disabled for Stage C testing and use float32 via training override at CLI.
model:
  posterior:
    type: riemannian_rhmc
    rhmc_steps: 0
    rhmc_step_size: 0.01
  # Some components also read mirrored keys at model root
  rhmc_steps: 0
  rhmc_step_size: 0.01

training:
  model:
    posterior:
      type: riemannian_rhmc
      rhmc_steps: 0
      rhmc_step_size: 0.01

